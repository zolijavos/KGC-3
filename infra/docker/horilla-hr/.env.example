# ==============================================================================
# Horilla HR Environment Configuration
# KGC ERP Integration - Story 33-3
# ==============================================================================
# Copy this file to .env and update values before running docker compose
# ==============================================================================

# ==============================================================================
# REQUIRED - Must be set before starting
# ==============================================================================

# Django Secret Key (REQUIRED)
# Generate with: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
# Or use: openssl rand -base64 50
DJANGO_SECRET_KEY=your-super-secret-django-key-change-me

# ==============================================================================
# Server Configuration
# ==============================================================================

# Horilla App Port (default: 3003)
# Note: Horilla internally uses 8000, mapped to 3003 for KGC project
HORILLA_PORT=3003

# Site URL (used for links and CSRF)
SITE_URL=http://localhost:3003

# Django Debug Mode (set to False in production!)
DJANGO_DEBUG=False

# Allowed Hosts (comma-separated)
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,horilla-app

# CSRF Trusted Origins (comma-separated, include protocol)
CSRF_TRUSTED_ORIGINS=http://localhost:3003,http://127.0.0.1:3003

# ==============================================================================
# Database Configuration
# ==============================================================================

# PostgreSQL Port (default: 5434 to avoid conflict with KGC/Twenty DB)
HORILLA_DB_PORT=5434

# PostgreSQL Credentials
POSTGRES_USER=horilla
POSTGRES_PASSWORD=horilla
POSTGRES_DB=horilla

# ==============================================================================
# Email Configuration (Optional)
# ==============================================================================

# SMTP Settings for sending emails
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=
# EMAIL_HOST_PASSWORD=
# EMAIL_USE_TLS=True

# ==============================================================================
# KGC Integration (Placeholder - for future integration)
# ==============================================================================

# KGC API URL (internal Docker network or host)
# KGC_API_URL=http://host.docker.internal:3000

# API Key for KGC integration
# KGC_API_KEY=

# Webhook secret for KGC callbacks
# KGC_WEBHOOK_SECRET=

# ==============================================================================
# Default Admin Credentials
# ==============================================================================
# Note: Horilla creates default admin on first run
# Username: admin
# Password: admin
# IMPORTANT: Change the admin password after first login!

# ==============================================================================
# Advanced Options
# ==============================================================================

# Gunicorn workers (default in image)
# GUNICORN_WORKERS=4

# Static files (handled by volumes)
# STATIC_ROOT=/app/staticfiles
# MEDIA_ROOT=/app/media
